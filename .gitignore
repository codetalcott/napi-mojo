node_modules/
build/
*.node
*.dylib
*.so
*.dll
# pixi environments
.pixi/*
!.pixi/config.toml
# macOS Finder metadata, at any depth
.DS_Store
# Editor config: settings.json is a SHARED cSpell dictionary of this project's
# jargon (bitcast, comptime, dlsym, tsfn, ...), so it is committed — without it
# every contributor's spell-checker flags the same two dozen domain words.
# Anything else under .vscode/ is machine-local.
.vscode/*
!.vscode/settings.json
# Claude session data
.claude/
# Agent skills (installed via npx skills)
.agents/
skills/
skills-lock.json
# Generated example build artifacts
docs/api-demo-addon/
examples/codegen/generated/
examples/codegen/build/
# Tutorial addon: generated/ and build/ are derived, CI regenerates them
examples/tutorial/generated/
examples/tutorial/build/
examples/tutorial/index.d.ts
# Kitchen-sink codegen compile target scratch output (regenerated every run)
tests/codegen/generated/
# Personal pinned-nightly note
.last-good-nightly
bench/napi-rs/target/
bench/napi-rs/index.js
bench/napi-rs/index.d.ts
